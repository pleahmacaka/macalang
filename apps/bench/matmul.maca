matmul(n: int) -> int {
    a = 0..(n * n)
    b = 0..(n * n)
    c = 0..(n * n)
    for i in 0..n - 1 {
        for j in 0..n - 1 {
            a[i * n + j] = (i + j) % 100
            b[i * n + j] = (i * j) % 100
        }
    }
    for i in 0..n - 1 {
        for j in 0..n - 1 {
            s = 0
            for k in 0..n - 1 {
                s = s + a[i * n + k] * b[k * n + j]
            }
            c[i * n + j] = s
        }
    }
    sum = 0
    for i in 0..n * n - 1 {
        sum = (sum + c[i]) % 1000000007
    }
    sum
}

main() -> int {
    info("{matmul(400)}")
    0
}
