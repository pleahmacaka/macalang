// HumanEval/23: Return length of given string
// Write it, and leave the test below alone.

strlen(string: str) -> int {
    fail "not written yet"
}

test_strlen() {
    assert_eq(strlen(""), 0, "case")
    assert_eq(strlen("x"), 1, "case")
    assert_eq(strlen("asdasnakj"), 9, "case")
}
