// HumanEval/13: Return a greatest common divisor of two integers a and b
// Write it, and leave the test below alone.

greatest_common_divisor(a: int, b: int) -> int {
    fail "not written yet"
}

test_greatest_common_divisor() {
    assert_eq(greatest_common_divisor(3, 7), 1, "case")
    assert_eq(greatest_common_divisor(10, 15), 5, "case")
    assert_eq(greatest_common_divisor(49, 14), 7, "case")
    assert_eq(greatest_common_divisor(144, 60), 12, "case")
}
