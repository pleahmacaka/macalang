// One `name = value` and no comma: a one-field record literal and a one-binding
// block read the same, so neither is taken.

Counter = { n: int }

start() -> Counter => { n = 0 }

main() -> int => start().n
