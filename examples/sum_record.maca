Shape = Dot | At(P)

P = {
    x: int
    y: int
}

f(s: Shape) -> int {
    match s {
        At(p) => p.x + p.y
        Dot => 0
    }
}

main() -> int {
    info("{f(At(P { x = 7, y = 5 }))}")
    info("{f(Dot)}")
    0
}
