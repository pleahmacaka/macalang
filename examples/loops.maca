// Imperative loops: `while`, `break`, `continue`, and reassignment.

sum_to(n: int) -> int {
    let acc = 0
    let i = 1
    while i <= n {
        acc = acc + i
        i = i + 1
    }
    acc
}

count_small(n: int) -> int {
    let steps = 0
    let i = n
    while i > 0 {
        i = i - 1
        if i > 100 {
            continue
        }
        steps = steps + 1
    }
    steps
}

main() -> int {
    info("{sum_to(10)}")
    info("{count_small(5)}")
    0
}
