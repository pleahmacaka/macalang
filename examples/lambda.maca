// An inline (non-capturing) lambda used as a function value. It is hoisted to a
// top-level function natively — no named helper needed (cf. examples/parallel.maca).

main() -> int {
    let xs = 1, 2, 3, 4
    let ys = xs.parallel(v => v * v)
    for y in ys {
        info("{y}")
    }
    0
}
