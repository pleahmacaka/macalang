slow_double(n: int) -> int {
    sleep_ms(50)
    n * 2
}

main() -> int {
    a = spawn slow_double(10)
    b = spawn slow_double(20)

    x = await a
    y = await b

    info("{x} + {y} = {x + y}")
    0
}
