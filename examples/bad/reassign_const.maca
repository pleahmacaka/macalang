// A `const` binding is immutable; reassigning it is an error. (A bare lowercase
// `count = 0` would be a mutable variable — drop the `const` to allow this.)
main() -> int {
    const count = 0
    count = count + 1
    info("{count}")
    0
}
