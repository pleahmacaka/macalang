// `titel` is a typo. Without the check it binds nothing, `title` stays empty,
// and the program runs.
Config = {
    host:  str
    title: str
}

main() -> int {
    c = Config { host = "localhost", titel = "Home" }
    info(c.title)
    0
}
